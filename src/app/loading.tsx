const page = () => {
    return (
        <div className="h-screen flex-col gap-4 w-full flex items-center justify-center">
            <div className="w-20 h-20 border-4 text-black text-4xl animate-spin border-gray-300 flex items-center justify-center border-t-black rounded-full"></div>
        </div>
    );
};

export default page;
